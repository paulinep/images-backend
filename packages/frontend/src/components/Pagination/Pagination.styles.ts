import { createStyles, Theme } from '@material-ui/core/styles'

export default (theme: Theme) => createStyles({
    root: {

    },
    page: {
        minWidth: 56,
        minHeight: 32,
        padding: 0,
        fontSize: 12,
    },
    page_active: {

    },
})
